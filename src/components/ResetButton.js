import React from "react";
import "../css/style.css";

const ResetButton = ({ value, isSelected }) => {
  return <button className="button button--reset">RESET</button>;
};

export default ResetButton;
